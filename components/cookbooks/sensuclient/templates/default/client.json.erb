{
   "client": {
      "name": "<%= @hostname %>",
         "address": "<%= @host_ip %>",
         "subscriptions": [ <%=  @subscriptions %> ],
    "team": "<%= @team %>",
    "keepalive": {
            "handlers": [ <%= @handlers %> ]
     },
    "alias": "<%= @alias %>",
    "nspath": "<%= @nspath %>",
    "application": "<%= @application %>"
   }
}
