# Docker release packages repo
#
# Auto generated by OneOps
# Created On : <%= Time.now.getutc %>

[dockerrepo]
name=Docker Repository
<%
  repo = node.docker_engine.repo
  repo = node.docker_engine.default_repo if repo.to_s.strip.empty?
  is_official_repo = repo.include?('https://yum.dockerproject.org')
%>
baseurl=<%= repo %>
enabled=1
<%
  if is_official_repo
%>
gpgcheck=1
gpgkey=https://yum.dockerproject.org/gpg
<% else %>
gpgcheck=0
<% end %>
