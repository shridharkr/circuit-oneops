###
# kubernetes scheduler config

# default config should be adequate

# Add your own!
KUBE_SCHEDULER_ARGS="<%= node['kube']['scheduler']['args'] %>"
KUBE_LOG_LEVEL="--v=<%= node.kubernetes.log_level %>
